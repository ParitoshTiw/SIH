# Ask UPAg for your permitted Open API endpoint/key, then set these before starting.
UPAG_API_URL=https://your-approved-upag-api-endpoint.example/market-data
UPAG_API_KEY=replace-with-server-side-secret

# Server port (default: 8000)
PORT=8000
